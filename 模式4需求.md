# ESP32-S3 加热控制系统 —— 模式 4 需求文档

> **模式 4：PID 稳定 + 全功率定长脉冲循环加热**
>
> 关联文档：《ESP32-S3 加热控制系统需求规格书(V2.0)》[需求.md](需求.md)
>
> 适用对象：加热功率极高的负载（实测约 200ms 升温 10℃），例如相变驱动器。

---

## 1. 设计基准

本章由实测加热功率推导出模式 4 的全部参数边界，是后续各章取值的唯一依据。

### 1.1 功率与时间基准

| 推导项 | 数值 | 说明 |
| --- | --- | --- |
| 加热速率 r | 10℃ / 200ms = **0.05 ℃/ms（50 ℃/s）** | 实测值 |
| 1 个控制周期（`APP_CONTROL_PERIOD_MS` = 20ms）对应温升 | **≈ 1℃** | 系统分辨率极限 |
| 10℃ 温区所需全功率加热时间 | **≈ 200ms = 10 个控制周期** | 脉冲时长的量级 |
| NTC 滤波滞后（2ms 采样 × 10 点滑动窗口） | ≈ 9ms ≈ 0.45℃ | 软件侧滞后 |
| 测量 + 控制总滞后 | ≈ 30~40ms ≈ **1.5~2℃ 表观滞后** | 软件滞后 + 驱动器响应 |

### 1.2 由基准导出的两条硬约束

**约束 1：加热时长与温区的关系**

```
heat_time ≈ ΔT(℃) × 20ms
```

即每 1℃ 需要 20ms 全功率加热。10℃ 温区对应 200ms，该值即为加热时长的上电默认值。

**约束 2：自适应容差不得小于 1℃**

脉冲宽度只能以控制周期（20ms）为步长调整，而 20ms 对应约 1℃ 的温升。若容差设为 0.5℃，自适应算法将在 ±20ms 之间反复跳动而无法收敛（极限环）。因此：

```
APP_MODE4_PEAK_TOL_C ≥ 1.0f
```

---

## 2. 状态机

模式 4 每路控制组独立运行一个三态状态机。

### 2.1 状态定义

| 状态 | PWM 输出 | 控制目标 | 退出条件 | 退出后 |
| --- | --- | --- | --- | --- |
| `MODE4_STABILIZE` | PID 输出（0~1000ms） | **T_low**（即 `requested_sp`） | \|T − T_low\| ≤ `APP_CYCLIC_HOLD_THRESHOLD_C` 且持续 `APP_CYCLIC_HOLD_TIME_MS` | → `MODE4_HEAT` |
| `MODE4_HEAT` | **恒定 1000ms（100% 全功率）** | 不参与控制，纯计时 | 计时达到 `heat_time_ms` | → `MODE4_COOL` |
| | | | **或** T ≥ T_high + `APP_MODE4_OVERTEMP_TRIP_C` | → `MODE4_COOL`（保护跳转） |
| `MODE4_COOL` | **0ms** | — | T ≤ T_low − `APP_MODE4_LOW_HYST_C`（含迟滞防抖） | → `MODE4_HEAT`，并按本次循环结果修正 `heat_time_ms` |

### 2.2 关键行为约定

1. **仅 `MODE4_STABILIZE` 阶段运行 PID**。进入 `MODE4_HEAT` 前必须调用一次 `ctrl_pid_reset()`，之后 PID 不参与任何运算，避免积分残留导致重新进入稳定阶段时产生冲量。
2. **温度设定值变更强制回到 `MODE4_STABILIZE`**。现有 `SP=` / `SETPOINT=` 接口改变 T_low 后，状态机复位回 `MODE4_STABILIZE`，重新用 PID 稳定到新的 T_low；模式 4 不增加命令。
3. **`MODE4_HEAT` 阶段为纯时间控制**，温度不参与决策。这是本模式"只改变加热时间"这一核心特征的实现方式。温度在该阶段仅有两处作用：
   - 过温跳闸（安全保护，见第 7 章）；
   - 记录峰值温度（供自适应算法使用）。
4. **峰值温度从 `MODE4_HEAT` 开始至本次 `MODE4_COOL` 结束持续取最大值**。断电后的上冲时长尚未实测，不能预设 500ms 窗口；在冷却段结束、准备开始下一次脉冲时才确定本次峰值并执行自适应修正。
5. **上电初始状态**为 `MODE4_STABILIZE`。

---

## 3. 温度接口

### 3.1 复用现有接口（不新增温度命令）

模式 4 的温度点完全沿用模式 1、模式 2 已有的两个温度接口，不新增任何温度相关命令。

| 角色 | 来源 | 类型 | 当前默认值 |
| --- | --- | --- | --- |
| **T_low**（低温点 + PID 稳定目标） | `requested_setpoint_c`，即 `SP=` / `SETPOINT=` 命令 | **运行期可变** | `APP_DEFAULT_SETPOINT_C` = 50℃ |
| **T_high**（脉冲峰值目标） | `APP_CYCLIC_SETPOINT2_C` | **编译期宏** | 60℃ |
| 到达判定阈值 | `APP_CYCLIC_HOLD_THRESHOLD_C` | 宏 | 0.5℃ |
| 稳定保持时间 | `APP_CYCLIC_HOLD_TIME_MS` | 宏 | 1000ms |

### 3.2 温度点配置方法

直接沿用模式1的默认温度和模式2的设定温度，进行循环。

### 3.3 合法性校验（强制）

模式 4 启动时必须校验 `T_high > T_low`。

**原因**：T_low 为运行期可变，若上位机发送 `SP=55` 而 T_high 仍为 50，状态机将陷入"加热永远达不到目标"的死循环。

**处理**：校验失败时强制退回 `MODE4_STABILIZE` 仅运行 PID，并输出一条告警日志，不进入循环加热。

---

## 4. 加热时长配置

### 4.1 默认值与工作值分离

加热时长由编译期默认值和运行期工作值组成，不新增上位机命令。

| 值 | 含义 | 允许被谁修改 |
| --- | --- | --- |
| `APP_MODE4_HEAT_TIME_DEFAULT_MS` | **默认值**：每次上电学习的起点 | 仅在编译期修改 |
| `heat_time_ms` | **工作值**：上电取默认值，自适应算法实际调整的对象 | 上电及温度设定变化时复位，其余时间仅由自适应算法修改 |

### 4.2 复位规则（强制）

满足以下任一条件时，工作值立即复位回编译期默认值，并回到 `MODE4_STABILIZE` 重新开始：

- 现有温度设定值变化（沿用已有 `SP=` / `SETPOINT=`，不新增或改变命令）。

**原因**：学到的加热时长只对「一组 (T_low, T_high) + 一种负载」有效，温度或负载变化后应重新学习。更换负载时可重启设备使工作值回到默认值。

### 4.3 命令约束

模式 4 不增加 `HTIME` 或其他上位机命令。加热时长只通过以下配置宏设定上电默认值，运行中由自适应算法调整。

### 4.4 配置宏

```c
#define APP_MODE4_HEAT_TIME_DEFAULT_MS   200.0f   // 上电默认加热开度时间 ≈ ΔT × 20ms
#define APP_MODE4_HEAT_TIME_MIN_MS       20.0f    // 自适应下限，1 个控制周期
#define APP_MODE4_HEAT_TIME_MAX_MS       1000.0f  // 自适应上限，超出说明功率/散热假设不成立
#define APP_MODE4_ADAPT_ENABLE           1        // 1=启用自适应；0=固定使用上电默认值
#define APP_MODE4_ADAPT_MAX_STEP_RATIO   0.30f    // 单次调整幅度上限（相对当前值）
#define APP_MODE4_PEAK_TOL_C             1.0f     // 峰值容差，不得小于 1℃（见 1.2 约束 2）
#define APP_MODE4_OVERTEMP_TRIP_C        3.0f     // 过温跳闸余量（相对 T_high）
#define APP_MODE4_HEAT_TIMEOUT_FACTOR    2.0f     // 加热超时系数（相对 heat_time）
#define APP_MODE4_LOW_HYST_C             0.5f     // 低温点触发迟滞，防抖
```

---

## 5. 自适应算法

### 5.1 算法步骤

每个循环结束（冷却至低温触发点、即将开始下一次脉冲前，确定本次最高温度 `T_peak`）修正一次加热时长：

```
r  = (MODE4_HEAT 段末温度 − MODE4_HEAT 段起始温度) / heat_time_ms   // 实测升温斜率，℃/ms
Δ  = (T_high − T_peak) / r                                          // 一步牛顿修正，单位 ms
Δ  = 量化到 20ms 的整数倍，且 |Δ| ≥ 20ms                            // 否则不会产生实际调整
Δ  = clamp(Δ, −heat_time_ms × APP_MODE4_ADAPT_MAX_STEP_RATIO,
              +heat_time_ms × APP_MODE4_ADAPT_MAX_STEP_RATIO)
heat_time_ms = clamp(heat_time_ms + Δ,
                     APP_MODE4_HEAT_TIME_MIN_MS,
                     APP_MODE4_HEAT_TIME_MAX_MS)
```

### 5.2 收敛判据

```
|T_peak − T_high| ≤ APP_MODE4_PEAK_TOL_C
```

满足后停止调整，并标记为"已收敛"状态。

### 5.3 计算示例

条件：T_low = 40℃，T_high = 50℃，基准加热时长 200ms，实测升温斜率 0.05 ℃/ms。

| 循环 | heat_time | 实测峰值 T_peak | Δ 计算 | 调整后 |
| --- | --- | --- | --- | --- |
| 1 | 200ms | 47.0℃ | (50 − 47) / 0.05 = 60ms | 260ms |
| 2 | 260ms | 50.3℃ | \|0.3\| < 1.0（容差内） | **收敛，停止调整** |

典型收敛时间为 2 个循环。

### 5.4 饱和处理

若 `heat_time_ms` 达到上限 `APP_MODE4_HEAT_TIME_MAX_MS` 后仍无法达到 T_high（说明功率假设不成立或散热异常），必须输出告警日志，不得静默处理。

---

## 6. 加热时长的上电行为

### 6.1 纯 RAM，每次上电重新学习

每次上电将各路 `heat_time_ms` 初始化为 `APP_MODE4_HEAT_TIME_DEFAULT_MS`，从第一次循环开始重新学习。学习结果只保存在 RAM 中；本次模式 4 修改不读取、不写入 NVS，也不改变现有 NVS 初始化及其他用途。断电后不保留已收敛的加热时长。

---

## 7. 安全保护

### 7.1 核心原则

本系统的加热速率为 50℃/s，即**每 1 个控制周期（20ms）温度上升约 1℃**。在此功率等级下，任何以时间为主要判据的保护都过于迟缓——若设置 30 秒级的加热超时，故障时温度早已远超安全范围。因此保护必须以**温度**为主判据。

### 7.2 保护项清单

| 保护项 | 阈值 | 实现要求 |
| --- | --- | --- |
| **过温跳闸** | T ≥ T_high + `APP_MODE4_OVERTEMP_TRIP_C`（默认 +3℃） | **每个控制周期（20ms）检查一次**，触发后立即关断并进入 `MODE4_COOL`。这是本模式的兜底保护 |
| 加热超时 | `APP_MODE4_HEAT_TIMEOUT_FACTOR × heat_time_ms`（默认 400ms） | 相对超时，禁止使用固定值（如 30s） |
| `heat_time` 上限 | `APP_MODE4_HEAT_TIME_MAX_MS` = 1000ms | 理论上限，对应 50℃ 温升，与过温跳闸构成双保险 |
| 传感器失效 | 沿用现有逻辑 | `periph_pwm_force_off_ch()` 已覆盖：温度无效时强制关断对应 PWM |
| 欠压保护 | 沿用现有逻辑 | `control_task` 已覆盖：`undervoltage` 时强制输出 0 |
| OTA 期间 | 沿用现有逻辑 | `control_task` 已覆盖：`ota_pending` 时强制输出 0 |
| T_low ≥ T_high | 模式 4 启动校验 | 退回 PID 模式并告警，见 3.3 |

### 7.3 硬件安全性说明

`MODE4_HEAT` 阶段的 100% 占空比在电气上表现为持续导通，不存在高频开关动作与开关损耗，对 NMOS 直接驱动的加热负载是安全的。
